import { registerRootComponent } from 'expo';
import App from '@/app/index'; // Adjust the path to your main App component.

registerRootComponent(App); // This ensures the app is registered correctly.
